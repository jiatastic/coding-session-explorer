[package]
name = "coding-session-explorer-tauri"
version = "0.1.0"
description = "Session explorer sidecar host"
edition = "2021"

[dependencies]
tauri = { version = "2.0", features = [] }

[build-dependencies]
tauri-build = { version = "2.0", features = [] }
